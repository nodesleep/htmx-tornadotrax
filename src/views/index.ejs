<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Tornado Data</title>
        <script src="https://unpkg.com/htmx.org@1.5.0/dist/htmx.min.js"></script>
        <style>
            * {
                box-sizing: border-box;
                padding: 0;
                margin: 0;
            }
            body {
                font-family: Arial, sans-serif;
                min-height: 100vh;
                color: rgb(48, 108, 212);
                background: linear-gradient(180deg, #000, rgb(26, 58, 114));
                padding: 10px;
                display: flex;
                flex-direction: column;
                justify-content: center;
            }
            table {
                width: 100%;
                border-collapse: collapse;
                background: #14171f;
                text-align: center;
                border-radius: 10px;
                padding: 10px;
            }
            th,
            td {
                font-size: 0.9rem;
                text-transform: uppercase;
                padding: 10px;
                color: #5e697d;
            }
            th {
                font-size: 0.7rem;
                color: #afafaf;
            }
            tr {
                border-bottom: 0.5px solid #262b33;
            }
            form {
                margin-block: 20px;
            }
            label {
                text-transform: uppercase;
                font-size: 0.8rem;
                font-weight: bold;
                margin-right: 10px;
            }
            input,
            select {
                margin-right: 10px;
                background: #14171f;
                color: #afafaf;
                border: none;
                padding: 10px;
                border-radius: 10px;
            }
            button {
                margin-right: 10px;
                background: rgb(48, 108, 212);
                color: #c9c9c9;
                border: none;
                padding-inline: 10px;
                padding-block: 8px;
                border-radius: 10px;
                font-weight: bold;
            }
            .container {
                width: 70rem;
                margin: auto;
            }
            .table-container {
                max-height: 50vh;
                overflow-y: auto;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>TornadoTrax HTMX</h1>
            <%- include('partials/_form', { query }) %>
            <div id="tornadoData">
                <div class="table-container">
                    <%- include('partials/_table', { data }) %>
                </div>
            </div>
        </div>
    </body>
</html>
